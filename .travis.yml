sudo: required

services:
  - docker

language: csharp

branches:
  only:
  - master

env:
  global:
    - COMMIT=${TRAVIS_COMMIT::8}

deploy:
  provider: script
  script: ./Publish.sh
  on:
    tags: true
#script:
#  ./Publish.sh
